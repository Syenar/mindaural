{
  "name": "mindaural",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first browser binaural beat creation, analysis and research workstation.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node scripts/test.mjs",
    "gauntlet": "node scripts/gauntlet.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
