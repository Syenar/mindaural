import {StereoBuffer} from '../core/types.js';
function str(v:DataView,o:number,s:string){for(let i=0;i<s.length;i++)v.setUint8(o+i,s.charCodeAt(i));}
export function encodeWav(buf:StereoBuffer,bits:16|24|32|'f32'=24){const bps=bits==='f32'?32:bits,bytes=bps/8,frames=buf.left.length,dataBytes=frames*2*bytes,ab=new ArrayBuffer(44+dataBytes),v=new DataView(ab);str(v,0,'RIFF');v.setUint32(4,36+dataBytes,true);str(v,8,'WAVEfmt ');v.setUint32(16,16,true);v.setUint16(20,bits==='f32'?3:1,true);v.setUint16(22,2,true);v.setUint32(24,buf.sampleRate,true);v.setUint32(28,buf.sampleRate*2*bytes,true);v.setUint16(32,2*bytes,true);v.setUint16(34,bps,true);str(v,36,'data');v.setUint32(40,dataBytes,true);let o=44;for(let i=0;i<frames;i++)for(const x0 of [buf.left[i],buf.right[i]]){const x=Math.max(-1,Math.min(1,x0));if(bits==='f32'){v.setFloat32(o,x,true);o+=4;}else if(bits===16){v.setInt16(o,Math.round(x<0?x*32768:x*32767),true);o+=2;}else if(bits===24){let q=Math.round(x<0?x*8388608:x*8388607);v.setUint8(o,q&255);v.setUint8(o+1,(q>>8)&255);v.setUint8(o+2,(q>>16)&255);o+=3;}else{v.setInt32(o,Math.round(x<0?x*2147483648:x*2147483647),true);o+=4;}}return new Uint8Array(ab);}
export function decodeWav(bytes:Uint8Array):StereoBuffer{const v=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);if(String.fromCharCode(...bytes.slice(0,4))!=='RIFF'||String.fromCharCode(...bytes.slice(8,12))!=='WAVE')throw new Error('Not a RIFF/WAVE file');let p=12,fmt=0,ch=0,sr=0,bits=0,dataOff=0,dataLen=0;while(p+8<=bytes.length){const id=String.fromCharCode(...bytes.slice(p,p+4)),len=v.getUint32(p+4,true);if(id==='fmt '){fmt=v.getUint16(p+8,true);ch=v.getUint16(p+10,true);sr=v.getUint32(p+12,true);bits=v.getUint16(p+22,true);}if(id==='data'){dataOff=p+8;dataLen=len;break;}p+=8+len+(len&1);}if(ch!==2||![1,3].includes(fmt)||![16,24,32].includes(bits))throw new Error('Unsupported WAV encoding');const bps=bits/8,frames=Math.floor(dataLen/(ch*bps)),l=new Float32Array(frames),r=new Float32Array(frames);let o=dataOff;const read=()=>{if(fmt===3){const x=v.getFloat32(o,true);o+=4;return x;}if(bits===16){const x=v.getInt16(o,true)/32768;o+=2;return x;}if(bits===24){let q=v.getUint8(o)|(v.getUint8(o+1)<<8)|(v.getUint8(o+2)<<16);if(q&0x800000)q|=0xff000000;o+=3;return q/8388608;}const x=v.getInt32(o,true)/2147483648;o+=4;return x;};for(let i=0;i<frames;i++){l[i]=read();r[i]=read();}return {sampleRate:sr,left:l,right:r,duration:frames/sr};}
export function encodeMonoWav(samples:Float32Array,sampleRate:number,bits:16|24|32|'f32'=24){const bps=bits==='f32'?32:bits,bytes=bps/8,dataBytes=samples.length*bytes,ab=new ArrayBuffer(44+dataBytes),v=new DataView(ab);str(v,0,'RIFF');v.setUint32(4,36+dataBytes,true);str(v,8,'WAVEfmt ');v.setUint32(16,16,true);v.setUint16(20,bits==='f32'?3:1,true);v.setUint16(22,1,true);v.setUint32(24,sampleRate,true);v.setUint32(28,sampleRate*bytes,true);v.setUint16(32,bytes,true);v.setUint16(34,bps,true);str(v,36,'data');v.setUint32(40,dataBytes,true);let o=44;for(const x0 of samples){const x=Math.max(-1,Math.min(1,x0));if(bits==='f32'){v.setFloat32(o,x,true);o+=4}else if(bits===16){v.setInt16(o,Math.round(x<0?x*32768:x*32767),true);o+=2}else if(bits===24){const q=Math.round(x<0?x*8388608:x*8388607);v.setUint8(o,q&255);v.setUint8(o+1,(q>>8)&255);v.setUint8(o+2,(q>>16)&255);o+=3}else{v.setInt32(o,Math.round(x<0?x*2147483648:x*2147483647),true);o+=4}}return new Uint8Array(ab);}
