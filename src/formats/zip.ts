const te=new TextEncoder();
function crc32(data:Uint8Array){let c=0xffffffff;for(const b of data){c^=b;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0);}return (c^0xffffffff)>>>0;}
function u16(a:number[],v:number){a.push(v&255,(v>>>8)&255);}function u32(a:number[],v:number){u16(a,v&65535);u16(a,v>>>16);}
export function createZip(entries:Record<string,Uint8Array|string>){const out:number[]=[],central:number[]=[];let offset=0,count=0;for(const [name,val] of Object.entries(entries)){if(name.includes('..')||name.startsWith('/')||name.includes('\\'))throw new Error('Unsafe ZIP path');const n=te.encode(name),d=typeof val==='string'?te.encode(val):val,crc=crc32(d);const local:number[]=[];u32(local,0x04034b50);u16(local,20);u16(local,0);u16(local,0);u16(local,0);u16(local,0);u32(local,crc);u32(local,d.length);u32(local,d.length);u16(local,n.length);u16(local,0);local.push(...n,...d);out.push(...local);const c:number[]=[];u32(c,0x02014b50);u16(c,20);u16(c,20);u16(c,0);u16(c,0);u16(c,0);u16(c,0);u32(c,crc);u32(c,d.length);u32(c,d.length);u16(c,n.length);u16(c,0);u16(c,0);u16(c,0);u16(c,0);u32(c,0);u32(c,offset);c.push(...n);central.push(...c);offset+=local.length;count++;}const start=out.length;out.push(...central);const end:number[]=[];u32(end,0x06054b50);u16(end,0);u16(end,0);u16(end,count);u16(end,count);u32(end,central.length);u32(end,start);u16(end,0);out.push(...end);return new Uint8Array(out);}
export function readStoredZip(bytes:Uint8Array,maxEntries=256,maxTotal=256*1024*1024){const dv=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength),td=new TextDecoder(),res:Record<string,Uint8Array>={};let p=0,total=0,count=0;while(p+30<=bytes.length&&dv.getUint32(p,true)===0x04034b50){const method=dv.getUint16(p+8,true),size=dv.getUint32(p+18,true),nameLen=dv.getUint16(p+26,true),extra=dv.getUint16(p+28,true);if(method!==0)throw new Error('Compressed ZIP entries are not supported by the safe project reader');const name=td.decode(bytes.slice(p+30,p+30+nameLen));if(name.includes('..')||name.startsWith('/')||name.includes('\\')||res[name])throw new Error('Unsafe or duplicate ZIP path');const start=p+30+nameLen+extra,end=start+size;if(end>bytes.length)throw new Error('Truncated ZIP');total+=size;if(++count>maxEntries||total>maxTotal)throw new Error('ZIP safety limit exceeded');res[name]=bytes.slice(start,end);p=end;}return res;}
