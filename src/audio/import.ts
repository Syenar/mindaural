import {StereoBuffer} from '../core/types.js';import {decodeWav} from '../formats/wav.js';import {decodeAiff} from '../formats/aiff.js';import {decodeFlacVerbatim} from '../formats/flac.js';
const banned=/\.(m4a|aac|mp4)$/i;
export function allowedAudioName(name:string){return /\.(wav|wave|aif|aiff|flac|mp3|ogg|oga|opus|webm)$/i.test(name)&&!banned.test(name);}
export async function decodeAudioBytes(bytes:Uint8Array,name:string,mime=''):Promise<StereoBuffer>{if(banned.test(name)||/aac|mp4/i.test(mime))throw new Error('AAC/M4A is intentionally unsupported');if(/\.wav$|\.wave$/i.test(name)||/audio\/wav/i.test(mime))return decodeWav(bytes);if(/\.aif$|\.aiff$/i.test(name)||/audio\/(aiff|x-aiff)/i.test(mime))return decodeAiff(bytes);if(/\.flac$/i.test(name)||/audio\/flac/i.test(mime)){try{return decodeFlacVerbatim(bytes);}catch{/* Use the browser decoder for externally encoded FLAC variants. */}}const AC=(window.AudioContext||(window as any).webkitAudioContext);if(!AC)throw new Error('No browser audio decoder is available');const ctx=new AC();try{const copy=bytes.slice().buffer,ab=await ctx.decodeAudioData(copy),l=new Float32Array(ab.length),r=new Float32Array(ab.length);ab.copyFromChannel(l,0);ab.copyFromChannel(r,Math.min(1,ab.numberOfChannels-1));return {sampleRate:ab.sampleRate,left:l,right:r,duration:ab.duration};}finally{await ctx.close();}}
