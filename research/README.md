# Local Research Cache

This directory is a curated **summary cache**, not a mirror of copyrighted papers. The detailed local science is in `docs/science/`.

`source_index.json` contains stable metadata/URLs/DOIs so future work can identify the exact primary source without repeating discovery research.
